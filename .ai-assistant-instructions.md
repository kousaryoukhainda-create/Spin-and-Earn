# General Instructions for AI Assistant

## ⚠️ CRITICAL: Before Starting Any Task

### 1. STOP and READ this file first
- Always consult this file before executing any command
- Follow the workflow below for EVERY task

### 2. ASK Before Making Changes
- **DO NOT** modify files without explicit permission
- **DO NOT** run long builds/compiles without asking
- **DO NOT** assume what the user wants - ASK first
- **DO NOT** make multiple changes in sequence without confirmation
- **DO NOT** execute commands that take >30 seconds without warning

### 3. GATHER Information First
Before any action, collect:
- [ ] What is the actual error/crash? (get logs)
- [ ] What has already been tried?
- [ ] What is the expected outcome?
- [ ] What tools/environment are available?
- [ ] User's device setup (phone only? computer connected?)

### 4. MINIMAL Approach
- Start with the **simplest possible solution**
- Make **one change at a time**
- **Verify** before proceeding to next step
- If stuck for >2 steps, **ask for help**

---

## 📋 Standard Workflow

### For Debug/Crash Issues:
```
1. ASK: "Can you share the crash logs?"
2. ASK: "How are you testing? (phone only / with computer / emulator)"
3. ASK: "What exactly happens when you launch?"
4. ONLY THEN: Analyze and suggest ONE fix
5. WAIT for confirmation before making changes
```

### For Build Issues:
```
1. ASK: "What is the exact error message?"
2. ASK: "Show me your current setup (SDK, Gradle versions)"
3. ASK: "Do you have permission to install SDK components?"
4. Suggest minimal fix, wait for confirmation
```

### For New Features/Changes:
```
1. ASK: "What exactly do you want to achieve?"
2. ASK: "Are there any constraints I should know?"
3. Propose a plan (3-5 steps max)
4. WAIT for approval before starting
5. Execute ONE step, then report back
```

---

## 🚫 What NOT to Do

### NEVER:
- ❌ Run `./gradlew build` or similar long commands without asking
- ❌ **NEVER trigger builds locally** - limited hardware/software/resources available
- ❌ Modify build.gradle.kts without permission
- ❌ Change SDK versions, compile versions, etc. without asking
- ❌ Create/modify configuration files without asking
- ❌ Assume the user has certain tools installed (adb, Android Studio, etc.)
- ❌ Make more than 1 file change without confirmation
- ❌ Run commands that take >30 seconds without warning
- ❌ Make assumptions about the user's environment
- ❌ **Add/delete any file or folder without explicit user permission**

### ALWAYS:
- ✅ Ask for crash logs first
- ✅ Ask about user's testing setup (phone only? computer?)
- ✅ Check what already exists before creating
- ✅ Propose before executing
- ✅ Keep changes minimal and reversible
- ✅ Report progress after each step
- ✅ Stop and ask if something unexpected happens
- ✅ Update HISTORY.md after every action

---

## 🎯 Communication Style

### Be Direct:
- Get straight to the point
- No long explanations unless asked
- 1-3 sentences per response when possible

### Ask Questions:
- "Can you share the crash logs?"
- "Should I proceed with X?"
- "Do you have Y available?"
- "Are you testing on phone or computer?"

### Report Clearly:
- What I did
- What happened
- What's next (if anything)

---

## 📁 Project-Specific Notes

### For This Spin and Earn Project:
- **Project Type:** Flutter app (spin wheel rewards)
- **Package Name:** `com.example.spin_to_earn`
- **User tests on physical phone only** (no computer/adb connection for testing)
- GitHub credentials saved: `~/.git-credentials` (kousaryoukhainda-create + GHP token)
- **Use GitHub API with token for programmatic access** - never ask user to check GitHub manually
- **Build via GitHub Actions only** - no local builds (limited resources)

### 📦 Key Dependencies:
- Firebase (Core, Auth, Firestore)
- Google Sign-In
- Google Mobile Ads (AdMob)
- flutter_fortune_wheel (spin wheel UI)
- Provider (state management)

---

## 🛠️ Environment Tools Available

### Development
| Tool | Version | Status |
|------|---------|--------|
| Java | 17.0.7 (OpenJDK) | ✅ |
| Node.js | Latest | ✅ |
| npm | Latest | ✅ |
| Python | 3.6.9 | ✅ |
| Git | Available | ✅ |
| Gradle Wrapper | Project `gradlew` | ✅ |

### Network
| Tool | Version | Status |
|------|---------|--------|
| curl | 7.58.0 | ✅ Fixed |
| wget | 1.19.4 | ✅ Fixed |

### Browser Automation
| Tool | Version | Notes |
|------|---------|-------|
| Chromium | 112.0.5615.49 | ✅ Headless mode |
| ChromeDriver | 112.0.5615.49 | ✅ |
| Xvfb | 2:1.19.6 | ✅ Virtual display |
| Selenium | 3.8.0 | ⚠️ ARM64 issues - use `browser_automation.sh` |

### Analysis / Reverse Engineering
| Tool | Version | Status |
|------|---------|--------|
| Radare2 | 2.3.0 | ✅ |
| strings | binutils | ✅ |
| objdump | binutils | ✅ |
| Ghidra | - | ❌ Not installed |
| APKTool | - | ❌ Not installed |
| JADX | - | ❌ Not installed |

### Archive Tools
- ✅ unzip, tar, gzip
- ❌ zip (not installed)

### Not Available
- ❌ Android SDK (use GitHub Actions for builds)
- ❌ Flutter SDK
- ❌ jq, xmllint
- ❌ pip3 (cannot install Python packages)

---

## 🔧 Environment-Specific Scripts

### Browser Automation (use instead of Selenium)
```bash
./browser_automation.sh github     # Check GitHub Actions
./browser_automation.sh ghidra     # Get Ghidra release links
./browser_automation.sh test       # Test browser
./browser_automation.sh fetch URL  # Fetch any webpage
```

### libapp.so Analysis
```bash
./analyze_libapp.sh                # Extract strings from native libs
```

### Files That Exist:
- `/storage/internal_new/project/Spin and Earn/pubspec.yaml` (Flutter config)
- `/storage/internal_new/project/Spin and Earn/lib/` (Dart source code)
- `/storage/internal_new/project/Spin and Earn/android/` (Android platform files)
- `/storage/internal_new/project/Spin and Earn/assets/images/` (app assets)

### Files That May NOT Exist:
- Complete native libraries (may need to be uploaded separately)
- Full Android SDK (use GitHub Actions for builds)
- adb access for testing (user has phone only)

---

## ✅ Checklist Before Any Action

- [ ] Read this instructions file
- [ ] Read HISTORY.md for context
- [ ] Asked for necessary information (logs, environment, etc.)
- [ ] Understood the exact problem
- [ ] Proposed a minimal solution
- [ ] Got user confirmation
- [ ] Ready to execute ONE step only

---

## ✅ Before Sending ANY Response (MANDATORY)

**This checklist MUST be completed before every response:**

- [ ] **History file updated** with current entry
- [ ] **Entry number incremented** (check last entry in `.ai-history.md`)
- [ ] **Timestamp/status added** to history
- [ ] **Verify**: Open `.ai-history.md` and confirm entry exists

**This is non-negotiable. Skip this = failure.**

---

## 🤖 Automatic History Updates

### Git Post-Commit Hook (Automatic)
After every `git commit`, the history file is **automatically updated** via `.git/hooks/post-commit`.

**How it works:**
1. You make changes to the project
2. Run `git commit -m "your message"`
3. Post-commit hook runs `update_history.sh` automatically
4. History entry created with commit info

### Manual Update Script (For chat interactions)
For updates between commits (chat queries, instructions, etc.):

```bash
./update_history.sh "Entry title" "User request" "Assistant action" "Result" "Status"
```

**Example:**
```bash
./update_history.sh "Instructions File Updated" "User asked to update instructions" "Updated .ai-assistant-instructions.md with new workflow" "File updated successfully" "Complete"
```

### Workflow Summary
| Trigger | Method | Automatic? |
|---------|--------|------------|
| Git commit | Post-commit hook | ✅ Yes |
| Chat interaction | Manual script call | ❌ AI must call script |
| Before any response | AI checklist | ❌ AI must remember |

---

## 📝 File Update Rules

### `.ai-assistant-instructions.md` (This File)
- **Update ONLY when user explicitly asks** to update instructions
- Used for consultation before every new task

### `.ai-history.md`
- **Update in REAL-TIME** after every command/query/instruction
- No delay - update immediately after each interaction
- Track: user request, action taken, result, timestamp

---

## 📥 Download Procedure (MANDATORY)

**Whenever downloading any file/project, follow this workflow:**

### (A) Estimate First
- Estimate download file size
- Estimate download time based on current connection speed

### (B) If Download Slows Down
1. **Pause** the download
2. **Wait 5 seconds**
3. **Resume** the download from where it left off

### (C) Repeat
- Repeat steps (B) 1-2-3 if slowdown condition persists

### (D) Stop and Exit When
- (i) Download completes successfully, OR
- (ii) Download time exceeds **5x** of initial estimated time

---

**Last Updated:** 2026-03-15
**Status:** Active - Must be followed for all future tasks
